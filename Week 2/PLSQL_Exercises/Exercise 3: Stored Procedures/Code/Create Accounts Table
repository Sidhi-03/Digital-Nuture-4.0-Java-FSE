CREATE TABLE Accounts (
    AccountID NUMBER PRIMARY KEY,
    CustomerID NUMBER,
    AccountType VARCHAR2(20),
    Balance NUMBER,
    LastModified DATE
);
INSERT INTO Accounts VALUES (1, 101, 'Savings', 10000, SYSDATE);
INSERT INTO Accounts VALUES (2, 102, 'Checking', 8000, SYSDATE);
INSERT INTO Accounts VALUES (3, 103, 'Savings', 20000, SYSDATE);
COMMIT;
