# Step 1: Initialize Git repository (if not already done)
git init

# Step 2: Create a sample text file
echo "Welcome to Git" > welcome.txt

# Step 3: Add the file to staging area
git add welcome.txt

# Step 4: Commit the file
git commit -m "Initial commit with welcome.txt"

# Step 5: Set up the remote GitLab repository
git remote add origin https://gitlab.com/vyassidhi70-group/GitDemo.git

# Step 6: Push to remote for the first time
git push -u origin master

# Step 7: Create a .log file (should be ignored later)
echo "This is a sample log entry" > debug.log

# Step 8: Create a 'log' directory and add a log file inside it
mkdir log
echo "Log folder file" > log/test.log

# Step 9: Add ignore rules for all .log files and the log/ folder
echo "*.log" >> .gitignore
echo "log/" >> .gitignore

# Step 10: Add .gitignore to staging
git add .gitignore

# Step 11: Commit the .gitignore file
git commit -m "Ignore .log files and log folder via .gitignore"

# Step 12: Push changes to remote repository
git push origin master

# Step 13: Verify status (should show clean working tree)
git status
